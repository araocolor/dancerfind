# 2026-05-01 작업 로그

## 점검 요청
- 요청: `todo.md` 기준으로 진행된 부분/미진행 부분 체크
- 점검 일시: 2026-05-01 (Asia/Seoul)

## 핵심 결과 요약

### 1) 진행된 부분
- DB 스키마 보완 반영 확인
  - `classes.type`, `classes.is_modified`, `classes.view_count` 존재
  - `notifications.type`에 `modified` 포함
- 기획 결정 문서 정리 존재
  - `docs/planning_answers.md`에 Phase 0 결정사항 정리됨
- 프로젝트 기반 일부 완료
  - `.env.local.example`, `eslint.config.mjs`, `.prettierrc`, `package.json`의 `lint/format` 스크립트 확인
  - Supabase 클라이언트 기본 파일 존재 (`src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`)
- 라우트 스켈레톤 일부 생성
  - `/login`, `/classes`, `/classes/list`, `/classes/new`, `/classes/[id]`, `/mypage`, `/notifications`, `/profile/[userId]`

### 2) 미진행/부분 진행
- `docs/todo.md` 체크박스는 전반적으로 미체크 상태
- 홈(`/`)은 기본 Next 템플릿 화면으로 실제 서비스 UI 미구현
- 인증 핵심 플로우 미구현
  - 카카오 OAuth 연동, callback, 로그아웃, 온보딩
- `middleware.ts` 없음 (보호 경로 미적용)
- API/컴포넌트 실구현 거의 없음
  - `src/app/api/*`, `src/components/*`에 기능 파일 부족
- 문서 목표 라우트 일부 미구현
  - `/users/[id]`, `/onboarding`, `/admin`, `/classes/[id]/edit`, `/error`, `/not-found`
- 타입 불일치 1건
  - DB에는 `modified`가 있으나 `src/types/notification.ts`의 `NotificationType`에는 없음

### 3) 종합 판단
- 현재 상태는 "기획 + 스키마 + 초기 골격" 단계
- 기능 구현(인증/검색/개설/신청/알림/관리자)은 본격 착수 전 상태

