# dancefinder 개발 TODO

> 기획 완성도 50% 기준으로 작성. 코딩 착수 전 결정이 필요한 항목과 구현 작업을 순서대로 정리.
> 각 항목은 **[기획 결정]** 또는 **[개발 구현]** 으로 구분.

---

## 🔴 PHASE 0 — 코딩 전 반드시 결정할 기획 사항

### 0-1. [기획 결정] DB 스키마 누락 항목 보완
- [x] `classes` 테이블에 `type TEXT NOT NULL DEFAULT 'class' CHECK (type IN ('class', 'event'))` 필드 추가
- [x] `classes` 테이블에 `is_modified BOOLEAN NOT NULL DEFAULT FALSE` 필드 추가
- [x] `notifications.type` CHECK에 `'modified'` 값 추가
- [x] `classes` 테이블에 `view_count INTEGER NOT NULL DEFAULT 0` 필드 추가
- [ ] 위 변경사항을 `supabase/schema.sql`에 반영 ← 개발 착수 시 처리

### 0-2. [기획 결정] 비로그인 접근 권한 범위 ✅
- [x] 홈 / 검색 1·2단계 / 클래스 상세 / 회원 프로필 → 비로그인 접근 허용
- [x] 신청 버튼 → 로그인 페이지 이동
- [x] middleware.ts 보호 경로: `/classes/new`, `/mypage`, `/notifications`, `/admin`, `/onboarding`

### 0-3. [기획 결정] 지역 필터 옵션 목록 ✅
- [x] 전체 / 서울강남 / 서울강북 / 경기도 / 부산 / 대구 / 대전 / 광주 / 창원 / 전주 / 제주
- [x] 정적 상수 배열로 관리

### 0-4. [기획 결정] 추천 클래스 선정 로직 ✅
- [x] 로그인: 내 활동지역 + 선호 장르 기반 → 마감임박순
- [x] 비로그인: 전체 인기순 (view_count 기준)

### 0-5. [기획 결정] 인기순 정렬 기준 ✅
- [x] 인기순 = view_count 기준
- [x] 클래스 상세 진입 시 +1 (중복 방지 MVP 이후)

### 0-6. [기획 결정] 클래스 삭제 정책 ✅
- [x] 신청자 있으면 하드 삭제 불가 → 취소 상태로만 변경
- [x] 취소 상태에서 신청자 전원 취소 시 자동 삭제
- [x] 신청자 0명이면 즉시 삭제 허용

### 0-7. [기획 결정] 카카오맵 UI 패턴 ✅
- [x] 개설 시: 지도 아이콘 클릭 → 카카오맵 팝업 검색 → 위도/경도 자동 저장
- [x] 상세 페이지: 카카오맵 위젯 + 주소 텍스트 병행

### 0-8. [기획 결정] 카카오 알림톡 발송 시나리오 ✅
- [x] 신청 수령 시 → 개설자
- [x] 승인 완료 시 → 신청자
- [x] 클래스 수정/취소 시 → 승인된 신청자 전원
- [x] D-1 리마인더 → MVP 제외
- [x] 거절 없음 (미승인 = 암묵적 거절)
- [x] 알림톡 코드 구현하되 feature flag로 비활성화 (채널 미준비)

### 0-9. [기획 결정] 회원가입 온보딩 ✅
- [x] 최초 로그인 → `/onboarding` → 닉네임 + 활동지역 입력 → 홈
- [x] 닉네임 미입력 시 서비스 이용 제한

### 0-10. [기획 결정] 알림함 UI 형태 ✅
- [x] 헤더 종 아이콘 클릭 → `/notifications` 별도 페이지 이동
- [x] 읽지 않은 알림 수 배지 표시

### 0-11. [기획 결정] 주요 액션 후 UX 피드백 ✅
- [x] 클래스 개설 완료 → 토스트 + 클래스 상세 페이지 이동
- [x] 클래스 신청 완료 → 토스트 + 마이페이지 이동
- [x] 승인/거절 처리 → 즉시 신청자 목록 상태 갱신

### 0-12. [기획 결정] 관리자 페이지 기능 목록 ✅
- [x] URL: `/admin` (admin role만 접근)
- [x] 회원 목록 조회 / 등급 변경 (member ↔ pro)
- [x] 전체 클래스/이벤트 조회 및 강제 삭제
- [x] 프로 신청 목록 조회 + 승인/거절 (Q6 추가)
- [x] 공지 알림 전송 → MVP 이후

### 0-13. [기획 결정] 추가 결정 사항 (Q1~Q15) ✅
- [x] 검색: 무한 스크롤 / 20개씩 / 다음 20개 prefetch
- [x] 기본 검색 조건: 로그인 → profiles / 비로그인 → sessionStorage
- [x] 레벨 옵션: 입문 / 초급 / 중급 / 고급 / 올레벨
- [x] 닉네임: 중복 불허 / 실시간 중복 확인 API
- [x] 장르: 살사 / 바차타 / 페스티벌 / 이벤트 / 기타
- [x] 검색 2단계: 장르 칩 필터 추가
- [x] Application 상태: 신청대기 / 승인 / 취소 (거절 없음)
- [x] 마감 D-1 미승인 자동 취소: Vercel Cron MVP 포함
- [x] 1:1 클래스 정원: 개설자 직접 입력
- [x] 회원 프로필: 모집중 클래스만 표시
- [x] 실시간 알림: MVP 미포함
- [x] 이미지 없는 클래스: 장르별 기본 이미지 3장 / 카드형 480px

---

## 🟡 PHASE 1 — 프로젝트 기반 설정

### 1-1. [개발 구현] App Router 폴더 구조 설계 및 생성
- [ ] `/src/app` 라우팅 구조 확정 및 생성
  ```
  /app
    /(auth)
      /login
      /onboarding        ← 0-9 결정에 따라
    /(main)
      /page.tsx          ← 홈
      /search
        /page.tsx        ← 검색 1단계 (조건 선택)
        /results
          /page.tsx      ← 검색 2단계 (리스트)
      /classes
        /new/page.tsx    ← 클래스 개설
        /[id]/page.tsx   ← 클래스 상세
        /[id]/edit/page.tsx ← 클래스 수정
      /users
        /[id]/page.tsx   ← 회원 프로필
      /mypage/page.tsx   ← 마이페이지
      /notifications/page.tsx ← 알림함
    /admin               ← 관리자 (0-12 확정 후)
      /page.tsx
    /not-found.tsx
    /error.tsx
  ```
- [ ] `/src/components` 폴더 구조 확정
  ```
  /components
    /ui          ← 버튼, 입력창, 뱃지 등 원자 컴포넌트
    /layout      ← Header, Footer, BottomNav
    /class       ← ClassCard, ClassForm, ClassDetail 등
    /user        ← ProfileCard, ProfileForm
    /search      ← SearchFilter, SearchResults
    /notification ← NotificationList, NotificationItem
  ```
- [ ] `/src/lib`, `/src/hooks`, `/src/types`, `/src/utils` 역할 분리 확정

### 1-2. [개발 구현] 환경변수 세팅
- [ ] `.env.local` 파일 생성 및 필요 키 목록 정리
  ```
  NEXT_PUBLIC_SUPABASE_URL=
  NEXT_PUBLIC_SUPABASE_ANON_KEY=
  NEXT_PUBLIC_KAKAO_MAP_API_KEY=
  KAKAO_REST_API_KEY=           ← 알림톡, 소셜 로그인
  KAKAO_CLIENT_SECRET=
  NEXT_PUBLIC_SITE_URL=
  ```
- [ ] Vercel 환경변수 대시보드에 동일 키 등록 (Preview / Production 분리)
- [ ] `.gitignore`에 `.env.local` 포함 확인

### 1-3. [개발 구현] Supabase 프로젝트 설정
- [ ] Supabase 프로젝트 생성
- [ ] `schema.sql` 실행 (PHASE 0-1 수정 반영 후)
- [ ] Supabase Auth에서 카카오 OAuth Provider 활성화
  - 카카오 디벨로퍼 앱에서 REST API 키 / Redirect URI 등록
  - Supabase Auth → Providers → Kakao 설정
- [ ] Supabase Storage 버킷 생성
  - 버킷명: `class-images`
  - 접근 정책: 인증 사용자만 업로드, 전체 읽기 허용
- [ ] Supabase 클라이언트 초기화 파일 작성 (`/src/lib/supabase.ts`)

### 1-4. [개발 구현] ESLint / Prettier 설정
- [ ] `.eslintrc` 또는 `eslint.config.mjs` 규칙 확정
- [ ] `.prettierrc` 파일 작성 (탭 너비, 따옴표 방식, 세미콜론 등)
- [ ] `package.json`에 lint/format 스크립트 추가

### 1-5. [개발 구현] 공통 레이아웃 컴포넌트
- [ ] Header 컴포넌트 (로고, 검색 아이콘, 알림 종 아이콘, 로그인/프로필)
- [ ] 모바일 하단 네비게이션 바 (홈 / 검색 / 클래스 개설 / 마이페이지)
- [ ] 루트 layout.tsx에 폰트 및 글로벌 스타일 적용
- [ ] 로딩 상태 공통 처리 (Skeleton 컴포넌트 또는 Spinner)
- [ ] 에러 페이지 (`/not-found.tsx`, `/error.tsx`)
- [ ] 빈 상태(Empty State) 공통 컴포넌트

### 1-6. [개발 구현] 인증 미들웨어 설정
- [ ] `middleware.ts` 작성
  - 0-2에서 확정한 보호 경로 목록 기반
  - 비로그인 접근 차단 및 `/login` 리다이렉트
- [ ] 로그인 후 리다이렉트 경로 처리 (이전 페이지 또는 홈)

---

## 🟢 PHASE 2 — 인증 구현

### 2-1. [개발 구현] 카카오 소셜 로그인
- [ ] 로그인 페이지 또는 모달 UI 구현 (카카오 로그인 버튼)
- [ ] Supabase Auth `signInWithOAuth({ provider: 'kakao' })` 연동
- [ ] 로그인 콜백 처리 (`/auth/callback/route.ts`)
- [ ] 로그인 성공 후 profiles 테이블 자동 생성 확인 (트리거 동작 검증)

### 2-2. [개발 구현] 온보딩 흐름 (0-9 결정에 따라)
- [ ] 최초 로그인 감지 (nickname이 비어있으면 온보딩으로 이동)
- [ ] 닉네임 / 활동지역 입력 폼
- [ ] 완료 후 홈으로 이동

### 2-3. [개발 구현] 로그아웃
- [ ] Supabase `signOut()` 처리
- [ ] 로그아웃 후 홈 또는 로그인 페이지 이동

### 2-4. [개발 구현] 세션 관리
- [ ] Supabase 세션 SSR 처리 (`@supabase/ssr` 패키지 활용)
- [ ] 세션 만료 시 자동 리다이렉트 처리

---

## 🟢 PHASE 3 — 홈 페이지

### 3-1. [개발 구현] 홈 UI
- [ ] 검색 창 (상단) — 클릭 시 검색 1단계로 이동
- [ ] 추천 클래스 리스트 (하단) — 0-4에서 확정한 로직으로 조회
- [ ] ClassCard 컴포넌트 (장르, 제목, 레벨, 일시, 지역, 모집상태 표시)
- [ ] 비로그인 접근 시 UI 처리 (0-2 결정 반영)

### 3-2. [개발 구현] 홈 API
- [ ] 추천 클래스 조회 API (`/api/classes/featured` 또는 Supabase 직접 쿼리)
- [ ] 이미지 첫 번째 장 썸네일 표시 처리

---

## 🟢 PHASE 4 — 클래스 검색

### 4-1. [개발 구현] 검색 1단계 — 조건 선택 페이지
- [ ] 지역 필터 UI (0-3에서 확정한 지역 목록 기반 선택)
- [ ] 모집 상태 필터 (모집중 / 마감 / 전체)
- [ ] 클래스 구분 필터 (전체 / 일반 그룹 / 1:1)
- [ ] "기본 옵션으로 저장" 체크박스
- [ ] 사용자별 기본 검색 조건 저장 (로컬스토리지 또는 profiles 테이블)
- [ ] 기본 옵션 저장 상태이면 이 페이지 건너뛰고 2단계로 이동

### 4-2. [개발 구현] 검색 2단계 — 리스트 페이지
- [ ] 1단계 조건을 URL 쿼리스트링으로 전달 및 파싱
- [ ] 키워드 텍스트 검색란 (제목 + 설명 대상, Supabase `ilike` 쿼리)
- [ ] 정렬 선택 UI (최신순 / 마감임박순 / 인기순)
- [ ] 페이지네이션 또는 무한 스크롤 (0에서 별도 확정 필요)
- [ ] 검색 결과 없음 — 빈 상태 UI
- [ ] 필터 변경 시 리스트 즉시 갱신

### 4-3. [개발 구현] 검색 API
- [ ] Supabase 쿼리: 지역 / 상태 / 구분 / 키워드 / 정렬 조합 쿼리 작성
- [ ] 인기순 정렬 쿼리 (0-5 확정 기준 반영)

---

## 🟢 PHASE 5 — 클래스 개설 / 수정

### 5-1. [개발 구현] 클래스 개설 폼
- [ ] 전체 필드 입력 폼
  - 장르(살사/바차타/기타), 제목, 레벨, 일시(날짜+시간), 마감일
  - 정원, 비용(무료이면 0), 연락처, 설명본문
  - 클래스 구분 (일반 그룹 / 1:1)
  - 이벤트 여부 (프로/관리자만 표시)
- [ ] 장소 입력 (0-7에서 확정한 카카오맵 UI 패턴 반영)
  - 위도/경도 자동 추출 및 저장
- [ ] 이미지 업로드 (최대 5장)
  - 클라이언트 사이드 이미지 압축 (너비 1034px / 아이콘용 200px)
  - Supabase Storage 업로드 후 URL 저장
- [ ] 폼 유효성 검사 (필수 필드, 마감일 < 강습일 등)
- [ ] 개설 완료 후 이동 경로 처리 (0-11 반영)

### 5-2. [개발 구현] 클래스 수정 폼
- [ ] 기존 데이터 prefill 처리
- [ ] 수정 완료 시 `is_modified = true` 업데이트
- [ ] 승인된 신청자에게 `modified` 타입 알림 발송
- [ ] 리스트/상세에서 "수정됨" 뱃지 표시

### 5-3. [개발 구현] 클래스 삭제 (0-6 확정 후)
- [ ] 삭제 확인 모달
- [ ] 신청자 있을 경우 처리 로직 (0-6 결정 반영)
- [ ] 삭제 후 알림 발송 (0-6 결정 반영)
- [ ] 소프트 삭제 or 하드 삭제 구현

---

## 🟢 PHASE 6 — 클래스 상세

### 6-1. [개발 구현] 클래스 상세 페이지
- [ ] 전체 필드 표시 (이미지 갤러리, 장르, 레벨, 일시, 장소, 정원/현재 신청수, 비용, 설명)
- [ ] 개설자 닉네임 + 사진 표시 → 클릭 시 프로필 페이지 이동
- [ ] 지도 표시 (0-7 결정 반영, 카카오맵 위젯)
- [ ] "수정됨" 뱃지 표시 (`is_modified = true`일 때)
- [ ] 모집 상태 뱃지 (모집중 / 마감 / 취소)

### 6-2. [개발 구현] 신청 버튼 및 흐름
- [ ] [신청] 버튼 — 비로그인 시 처리 (0-2 반영)
- [ ] 이미 신청한 경우 "신청완료" 상태 표시
- [ ] 정원 초과 시 버튼 비활성화
- [ ] 신청 완료 후 UI 피드백 (0-11 반영)
- [ ] 취소 버튼 — 강습일 1일 이상 남았을 때만 활성화

### 6-3. [개발 구현] 신청자 관리 (개설자 전용)
- [ ] 클래스 상세 하단에 신청자 리스트 표시 (개설자 본인만 노출)
- [ ] 신청자별 승인 / 거절 버튼
- [ ] 승인 처리 시 신청자에게 `approved` 알림 발송
- [ ] 거절 처리 시 신청자에게 `cancelled` 알림 발송

---

## 🟢 PHASE 7 — 마이페이지

### 7-1. [개발 구현] 내 프로필 편집
- [ ] 닉네임, 프로필 사진, 자기소개(bio), 활동지역 수정 폼
- [ ] 프로필 이미지 업로드 (Supabase Storage `avatars` 버킷)
- [ ] 저장 완료 후 토스트 또는 즉시 반영

### 7-2. [개발 구현] 내가 개설한 클래스 목록
- [ ] 개설한 클래스 리스트 (상태별 필터: 모집중 / 마감 / 취소)
- [ ] 각 클래스에서 수정 / 삭제 버튼 접근 가능

### 7-3. [개발 구현] 내가 신청한 클래스 목록
- [ ] 신청 상태 표시 (신청대기 / 승인 / 취소)
- [ ] 취소 버튼 (강습일 1일 이상 남았을 때만 활성화)

### 7-4. [개발 구현] 알림 설정
- [ ] 카카오톡 알림 수신 여부 토글
- [ ] 설정 저장 (profiles 또는 별도 user_settings 테이블)

---

## 🟢 PHASE 8 — 회원 프로필 페이지

### 8-1. [개발 구현] 공개 프로필 페이지 (`/users/[id]`)
- [ ] 닉네임 + 프로필 사진
- [ ] 자기소개(bio)
- [ ] 활동 지역
- [ ] 해당 사용자가 개설한 클래스 목록 표시 (옵션)
- [ ] 본인 프로필 접근 시 "편집" 버튼 표시

---

## 🟢 PHASE 9 — 알림 시스템

### 9-1. [개발 구현] 사이트 내 알림함
- [ ] 0-10에서 확정한 UI 형태 구현 (페이지 or 드롭다운)
- [ ] 알림 목록 조회 (최신순, 읽음/안읽음 구분)
- [ ] 알림 클릭 시 `link_url`로 이동 + `is_read = true` 업데이트
- [ ] 헤더 종 아이콘에 읽지 않은 알림 수 배지 표시
- [ ] Supabase Realtime으로 실시간 알림 수신 (선택)

### 9-2. [개발 구현] 알림 발송 서버 로직
- [ ] 신청/승인/거절/수정/취소 이벤트 발생 시 `notifications` 테이블 INSERT
- [ ] 카카오 알림톡 발송 (0-8에서 확정한 트리거 시나리오 반영)
  - Next.js API Route에서 카카오 알림톡 REST API 호출
  - 발송 실패 시 fallback 처리 (사이트 알림만 저장)

---

## 🟢 PHASE 10 — 관리자 페이지

### 10-1. [개발 구현] 관리자 페이지 (`/admin`)
- [ ] 관리자 전용 접근 제한 (middleware에서 role === 'admin' 검사)
- [ ] 회원 목록 조회 및 등급 변경 (member ↔ pro)
- [ ] 전체 클래스/이벤트 조회, 강제 삭제
- [ ] 공지 알림 전송 기능 (전체 사용자 대상 notifications INSERT)

---

## 🔵 PHASE 11 — 마무리 및 배포

### 11-1. [개발 구현] SEO 및 메타태그
- [ ] 각 페이지 `metadata` 설정 (title, description)
- [ ] 클래스 상세 페이지 OG 태그 (소셜 공유 시 썸네일 포함)
- [ ] `robots.txt`, `sitemap.xml` 설정

### 11-2. [개발 구현] 성능 최적화
- [ ] Next.js `Image` 컴포넌트로 이미지 최적화
- [ ] 리스트 페이지 `Suspense` + `loading.tsx` 처리
- [ ] 불필요한 클라이언트 컴포넌트 최소화

### 11-3. [개발 구현] 배포 검증
- [ ] Vercel Preview 배포 확인
- [ ] 환경변수 Production 설정 확인
- [ ] Supabase RLS 정책 실제 동작 검증
- [ ] 카카오 소셜 로그인 Production 도메인 등록 (카카오 디벨로퍼 앱 리다이렉트 URI 추가)
- [ ] 모바일 브라우저 실제 기기 테스트

---

## 📊 완성도 현황

| Phase | 내용 | 상태 |
|---|---|---|
| Phase 0 | 기획 결정 사항 (12개 항목) | ⬜ 미완 |
| Phase 1 | 프로젝트 기반 설정 | ⬜ 미완 |
| Phase 2 | 인증 구현 | ⬜ 미완 |
| Phase 3 | 홈 페이지 | ⬜ 미완 |
| Phase 4 | 클래스 검색 | ⬜ 미완 |
| Phase 5 | 클래스 개설/수정 | ⬜ 미완 |
| Phase 6 | 클래스 상세 | ⬜ 미완 |
| Phase 7 | 마이페이지 | ⬜ 미완 |
| Phase 8 | 회원 프로필 | ⬜ 미완 |
| Phase 9 | 알림 시스템 | ⬜ 미완 |
| Phase 10 | 관리자 페이지 | ⬜ 미완 |
| Phase 11 | 마무리 및 배포 | ⬜ 미완 |

> **Phase 0 완료 후 Phase 1 착수 권장.**
> Phase 1~3은 병렬 진행 가능. Phase 4~10은 독립적으로 진행 가능하나 Phase 1 완료 후 시작.
